services:
  - type: web
    name: attendify-face-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: MONGODB_URL
        value: mongodb+srv://Krishna:Krish%40atlas25@attendify-cluster.fh96zp0.mongodb.net/attendifyDB?retryWrites=true&w=majority
